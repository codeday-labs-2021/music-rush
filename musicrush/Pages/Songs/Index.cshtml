@page
@model musicrush.Pages.Songs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Songs</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form class="row">
    <div class="search-bar">
        <input type="text" asp-for="SearchString" placeholder="search..." class="text-dark search-box" />
        <select asp-for="SongGenre" asp-items="Model.Genres" placeholder="Genre..." class="text-muted search-genre">
            <option value="">Genre</option>
        </select>
    </div>
    <button type="submit" value="Search" class="text-dark search-btn"><i class="fas fa-search"></i></button>
</form>
<table class="table">
    @* <thead>
        <tr>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Title)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].ReleaseDate)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Genre)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Artist)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Album)
        </th>
        <th></th>
        </tr>
        </thead> *@
    <div class="wrapper">
        @foreach (var item in Model.Song)
        {
            <a asp-page="./Details" asp-route-id="@item.ID">
                <div class="card">
                    <div class="row no-gutters">
                        <div class="col-sm-3">
                            <img src="https://picsum.photos/1000" class="card-img-top" alt="placeholder img">
                        </div>
                        <div class="col-sm-9">
                            <div class="card-body">
                                <h5 class="card-title">
                                    @Html.DisplayFor(modelItem => item.Title)
                                </h5>
                                @* <div class="col-2 text-box"><span>
                                    @Html.DisplayFor(modelItem => item.ReleaseDate)
                                    </span></div> *@
                                @* <div class="col-2 text-box"><span>
                                    @Html.DisplayFor(modelItem => item.Genre)
                                    </span></div> *@
                                <span class="card-text">
                                    @Html.DisplayFor(modelItem => item.Artist)
                                </span>
                                @* <div class="col-2 text-box"><span>
                                    @Html.DisplayFor(modelItem => item.Album)
                                    </span>
                                    </div>*@
                                @* <span>
                                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                                    </span> *@
                                <p class="rating">
                                    Rating: @Html.DisplayFor(modelItem => item.Rating)/5
                                </p>

                            </div>
                        </div>
                    </div>
                </div>
            </a>
        }
    </div>
</table>
