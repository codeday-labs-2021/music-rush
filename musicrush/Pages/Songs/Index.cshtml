@page
@model musicrush.Pages.Songs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form>
    <p>
        Title: <input type="text" asp-for="SearchString" placeholder="search..." class="text-dark" />
        <select asp-for="SongGenre" asp-items="Model.Genres" placeholder="Genre..." class="text-dark">
            <option value="">All</option>
        </select>
        <input type="submit" value="Filter" class="text-dark" />
    </p>
</form>
<table class="table">
    @* <thead>
        <tr>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Title)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].ReleaseDate)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Genre)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Artist)
        </th>
        <th>
        @Html.DisplayNameFor(model => model.Song[0].Album)
        </th>
        <th></th>
        </tr>
        </thead> *@
    <div class="wrapper">
        @foreach (var item in Model.Song)
        {
            <div class="card">
                <div class="row">
                    <div class="col-3 album-art">
                        <p>X</p>
                    </div>
                    <div class=col>
                        <div class="row text-box"><span>
                                @Html.DisplayFor(modelItem => item.Title)
                            </span></div>
                        @* <div class="col-2 text-box"><span>
                            @Html.DisplayFor(modelItem => item.ReleaseDate)
                            </span></div> *@
                        @* <div class="col-2 text-box"><span>
                            @Html.DisplayFor(modelItem => item.Genre)
                            </span></div> *@
                        <div class="row text-box"><span>
                                @Html.DisplayFor(modelItem => item.Artist)
                            </span></div>
                        @* <div class="col-2 text-box"><span>
                            @Html.DisplayFor(modelItem => item.Album)
                            </span></div> *@
                    </div>
                    <div class="col-2 text-box"><span>
                            <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                            <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                            <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                        </span>
                    </div>
                </div>
            </div>
        }
    </div>
</table>
